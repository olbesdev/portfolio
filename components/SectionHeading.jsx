import React from 'react'

const SectionHeading = ({children}) => {
  return (
    <h2 className='text-3xl text-slate-100 font-medium capitalize mb-10 text-center'>{children}</h2>
  )
}

export default SectionHeading
